# Exchange read-only policy

AgentFlow can report whether read-only credentials are configured for Binance, Bybit and OKX. The system must only use credentials whose permissions are limited to account/balance reads.

Never configure withdrawal, transfer, order/trading, or asset-management permissions. Never request or store seed phrases/private keys. Portfolio balance is informational and must never be counted as earned revenue.
